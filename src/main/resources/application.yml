server:
  port: 8080

spring:
  application:
    name: egov001

  datasource:
    url: jdbc:mariadb://localhost:3306/egovBoard?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: true

